import { Box } from "@mui/system";

const Search = () => {
   return (
      <Box
         width="100%"
         display="flex"
         alignItems="center"
         justifyContent="space-between"
         mb={2}
      >
        UserProfile
      </Box>
   );
};

export default Search;
